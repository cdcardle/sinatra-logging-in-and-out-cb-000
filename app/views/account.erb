<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= Helpers.current_user(session).username %>'s Account</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <a href="/">Home Page</a>
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome, <%= Helpers.current_user(session).username %></h1>
      <h2>Your Current Balance: <%= Helpers.current_user(session).balance %>
      <a href="/logout">Log Out</a>
    <% end %>
  </body>
</html>
