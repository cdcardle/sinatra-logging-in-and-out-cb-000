<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if User.is_logged_in? %>
    <%= "#{User.current_user.username}: #{current_user.balance}" %>
    <% else %>
    <a href="/">Home Page</a>
    <% end %>
    <a href="/logout">Log Out</a>
  </body>
</html>
